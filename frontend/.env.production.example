# Vercel → Project → Settings → Environment Variables
# Name: VITE_API_URL
# Value: your Railway backend URL (https only, NO trailing slash, NO /api)
#
# Example:
VITE_API_URL=https://delivery-navy-production.up.railway.app
#
# After saving variables you MUST click Redeploy — Vite embeds this at build time.
